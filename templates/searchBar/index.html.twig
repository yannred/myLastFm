{#  <div class="d-flex">#}
{#    {{ form_row(form.type) }}#}
{#    {{ form_row(form.from) }}#}
{#    {{ form_row(form.to) }}#}
{#    {{ form_row(form.trackName) }}#}
{#    {{ form_row(form.artistName) }}#}
{#    {{ form_row(form.albumName) }}#}
{#  </div>#}

<div class="my-3">
  {{ form_start(form) }}

  <div>
      {{ form_errors(form) }}
  </div>

  <div class="d-flex form-group">

    {% if
      not searchBar is defined
      or searchBar == 'full'
      or searchBar == ''
    %}
      {% set DateSearchVisibility = '' %}
      {% set StringSearchVisibility = '' %}

    {% elseif searchBar == 'date' %}
      {% set DateSearchVisibility = '' %}
      {% set StringSearchVisibility = 'inputFormInvisible' %}
    {% endif %}

    {{ form_row(form.type) }}

    {% include 'searchBar/lablel.html.twig' with {'input': form.from, 'visibility' : DateSearchVisibility} %}
    {% include 'searchBar/input.html.twig' with {'input': form.from, 'visibility' : DateSearchVisibility} %}

    {% include 'searchBar/lablel.html.twig' with {'input': form.to, 'visibility' : DateSearchVisibility} %}
    {% include 'searchBar/input.html.twig' with {'input': form.to, 'visibility' : DateSearchVisibility} %}

    {% include 'searchBar/lablel.html.twig' with {'input': form.trackName, 'visibility' : StringSearchVisibility} %}
    {% include 'searchBar/input.html.twig' with {'input': form.trackName, 'visibility' : StringSearchVisibility} %}

    {% include 'searchBar/lablel.html.twig' with {'input': form.artistName, 'visibility' : StringSearchVisibility} %}
    {% include 'searchBar/input.html.twig' with {'input': form.artistName, 'visibility' : StringSearchVisibility} %}

    {% include 'searchBar/lablel.html.twig' with {'input': form.albumName, 'visibility' : StringSearchVisibility} %}
    {% include 'searchBar/input.html.twig' with {'input': form.albumName, 'visibility' : StringSearchVisibility} %}

    {{ form_end(form, {'render_rest': false}) }}

    {{ form_widget(form._token) }}

    <div class="mx-3 d-flex align-items-center"><button type="submit" class="btn btn-sm btn-primary">Find</button></div>

  </div>

  {{ form_end(form) }}
</div>